name: File Size

on:
  push:
    branches: [main, master, dev]
  pull_request:

jobs:
  file-size:
    name: 200-line Python limit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: File size gate
        run: python scripts/check_file_size.py
