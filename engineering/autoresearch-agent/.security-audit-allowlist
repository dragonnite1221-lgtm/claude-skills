# Per-skill security-audit suppressions (auditable). Scoped to this skill only;
# detection is unchanged for every other skill. Pinned to exact file:line so any
# NEW command-execution pattern added to these templates still surfaces.
#
# No active suppressions. Benchmark commands are parsed with shlex and executed
# without a shell, so configured pipelines and metacharacters are never treated
# as executable shell syntax.
